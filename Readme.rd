围绕PlatON社区  写的领取收益和将收益委托到节点脚本

sudo apt-get update && sudo apt-get install python3-venv tmux git -y&& https://github.com/zhanglonglongSH/PlatonDelegate.git&& cd PlatonDelegate && python3 -m venv venv&& source ./venv/bin/activate && pip3 install -r requirements.txt

